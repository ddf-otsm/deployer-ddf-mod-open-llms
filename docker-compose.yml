# Generated docker-compose.yml
# DO NOT EDIT MANUALLY - Generated from config files
# To regenerate: bash scripts/generate-docker-compose.sh --env=dev --platform=docker

version: '3.8'

services:
  ai-testing-agent:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: deployer-ddf-mod-llm-models-dev
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - AUTH_DISABLED=true
      - PORT=3000
      - LOG_LEVEL=debug
      - OLLAMA_HOST=ollama:11434
    volumes:
      - ./logs:/app/logs
      - ./config:/app/config:ro
    networks:
      - ai-testing-network
    depends_on:
      - ollama
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  ollama:
    image: ollama/ollama:latest
    container_name: ollama-server
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - ai-testing-network
    restart: unless-stopped
    environment:
      - OLLAMA_HOST=0.0.0.0
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  # Optional: Redis for session storage (if needed later)
  redis:
    image: redis:7-alpine
    container_name: redis-cache
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - ai-testing-network
    restart: unless-stopped
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  ai-testing-network:
    driver: bridge
    name: ai-testing-network

volumes:
  ollama_data:
    name: ollama_data
  redis_data:
    name: redis_data

# Generated on: 2025-05-26T21:53:18Z
# From config: /Users/luismartins/cursor_local_repos/deployer-ddf-mod-open-llms/config/dev.docker.yml
